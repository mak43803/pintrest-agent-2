"""
Conversation History - Chat message storage and retrieval.

Manages the ordered sequence of user/assistant/system messages
that form the conversation context for LLM calls.
"""

# TODO: Implement ConversationHistory class
# - add_message(role, content) -> append a message
# - get_messages(limit) -> retrieve recent messages
# - get_full_history() -> retrieve all messages
# - clear() -> reset conversation history
# - to_llm_format() -> format messages for Ollama API
